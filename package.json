{
  "name": "gem-quest",
  "version": "1.1.0",
  "private": true,
  "description": "Production-ready HTML5 arena survival game for CrazyGames.",
  "scripts": {
    "start": "node serve.js",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "check:architecture": "node scripts/architecture.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test tests/*.test.mjs tests/unit/*.test.mjs tests/integration/*.test.mjs",
    "test:coverage": "c8 node --test tests/*.test.mjs tests/unit/*.test.mjs tests/integration/*.test.mjs",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "verify:dist": "node scripts/verify-dist.mjs",
    "verify": "npm run check && npm run typecheck && npm test && npm run build && npm run verify:dist",
    "verify:quick": "npm run check && npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "c8": "^11.0.0",
    "typescript": "^7.0.2"
  }
}
